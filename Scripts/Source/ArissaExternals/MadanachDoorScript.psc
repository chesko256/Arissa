ScriptName MadanachDoorScript extends ObjectReference

Event OnOpen(ObjectReference akActionRef)

;	If (akActionRef != Game.GetPlayer())
;		Utility.Wait(2)
;		Self.SetOpen(False)
;		Self.Lock()
;	EndIf

EndEvent